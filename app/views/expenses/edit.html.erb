<h1>Editing expense</h1>

<%= render "form", expense: @expense %>

<br>

<div>
  <%= link_to "Show this expense", user_expense_path(params[:user_id], @expense) %> |
  <%= link_to "Back to expenses", user_expenses_path %>
</div>
